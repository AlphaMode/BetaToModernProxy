pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            name "FabricMC"
            url "https://maven.fabricmc.net/"
        }
    }
}

rootProject.name = 'BetaToModernProxy'

include(":bootstrap")
include(":data-gen")

include(":plugins:fabric")
